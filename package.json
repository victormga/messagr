{
  "name": "@victormga/messagr",
  "version": "1.0.0",
  "description": "Messagr is a MOD (message oriented design) nodejs library for improved code organization.",
  "main": "./dist/index.ts",
  "scripts": {
	"build": "tsc -p tsconfig.json && tsc -p tsconfig-es5.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Victor Hugo Sabiar",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.10"
  }
}
